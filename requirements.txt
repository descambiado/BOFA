# BOFA core installation.
#
# Specialized scripts may require optional tools documented in their own
# metadata. Keep the default install small, reproducible and suitable for the
# API, CLI and verified flagship workflow.

-r api/requirements.txt
-r cli/requirements.txt
