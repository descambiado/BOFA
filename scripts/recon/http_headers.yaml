name: http_headers
category: recon
description: "Obtiene las cabeceras HTTP de una URL para fingerprinting y análisis de seguridad"
parameters:
  url:
    type: string
    description: "URL objetivo (ej. https://example.com)"
    required: true
  timeout:
    type: integer
    description: "Timeout en segundos"
    required: false
    default: 10
  json:
    type: boolean
    description: "Salida en formato JSON"
    required: false
    default: false
  insecure:
    type: boolean
    description: "No verificar certificado SSL (solo dev/test)"
    required: false
    default: false
